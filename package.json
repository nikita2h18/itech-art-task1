{
  "name": "iteach-art-tasks",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "prepare": "husky install",
    "lint": "prettier --check .",
    "format": "prettier --write ."
  },
  "husky": {
    "hooks": {
      "pre-commit": "npm lint && git add -a ."
    }
  },
  "devDependencies": {
    "husky": "^6.0.0",
    "prettier": "2.3.0"
  }
}
